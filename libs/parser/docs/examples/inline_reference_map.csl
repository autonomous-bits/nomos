# Inline References in Map/Collection Context
#
# This example shows how inline references work within nested map structures,
# allowing you to reference different properties for different keys.

source:
	alias: 'network'
	type: 'folder'
	path: './network-config'

source:
	alias: 'secrets'
	type: 'vault'
	path: './secrets'

servers:
	web:
		ip: @network:web.ip
		port: '8080'
		ssl_cert: @secrets:web.cert
	api:
		ip: @network:api.ip
		port: '3000'
		api_key: @secrets:api.key
	database:
		host: @network:db.host
		port: '5432'
		password: @secrets:db.password
